# user-data (Ubuntu 자동 설치)
#cloud-config
autoinstall:
  version: 1
  identity:
    hostname: ubuntu-svr
    password: "$6$exYm6SREtuiButp6$6Z7.7m..82HbaMrc0.S.S1Tz.vU1v89kY8x.0L766yU6Cdfk1" # Password123! 해시
    username: admin
  locale: ko_KR.UTF-8
  keyboard: {layout: kr}
  ssh:
    install-server: true
    allow-pw: true
  # [요청사항] 네트워크 설정
  network:
    network:
      version: 2
      ethernets:
        ens33:
          dhcp4: true
  # [요청사항] 포트 변경 및 방화벽
  late-commands:
    - sed -i 's/#Port 22/Port 60022/' /target/etc/ssh/sshd_config
    - curtin in-target -- ufw allow 60022/tcp
    - curtin in-target -- ufw enable
    